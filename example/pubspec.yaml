name: adaptive_kit_example
description: Example app demonstrating adaptive_kit package features.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  adaptive_kit:
    path: ../
  google_fonts: ^6.2.1
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
